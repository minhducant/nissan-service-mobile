import {Colors} from './colors';
import {FontFamily, FontSize, FontSizeNormalize} from './fonts';
import {SCREEN_HEIGHT, SCREEN_WIDTH, IconSize} from '@styles/variables';
import {authenticationStyles} from '@styles/authentication';
import {appointmentStyles} from '@styles/appointment';
import {
  receptionStyles,
  tableCheckOrderStyles,
  vehicleBodyStyle,
  modalCheckOrderSty,
  receptionDetailStyles,
} from '@styles/reception';
import css from '@styles/css';
import {quotationStyles} from '@styles/quotation';
import {progressStyle} from '@styles/progress';
import {vehicleHandingStyles} from '@styles/vehicle_handing';
import {reportStyles} from '@styles/report.styles';
import {advisoryStyles} from '@styles/advisory';
import {repairStyles} from '@styles/repair';
import {quickQuoteStyles} from '@styles/quick_quotation';
export {
  Colors,
  FontFamily,
  FontSize,
  FontSizeNormalize,
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
  authenticationStyles,
  appointmentStyles,
  IconSize,
  receptionStyles,
  quotationStyles,
  css,
  progressStyle,
  vehicleHandingStyles,
  tableCheckOrderStyles,
  vehicleBodyStyle,
  modalCheckOrderSty,
  receptionDetailStyles,
  reportStyles,
  advisoryStyles,
  repairStyles,
  quickQuoteStyles,
};
